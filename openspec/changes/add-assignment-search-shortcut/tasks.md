## Tasks

- [x] Add shared assignment search state and focus-request handling to the authenticated shell in `HomePage`.
- [x] Wire the dashboard `TopBar` search affordance to activate Assignments search and preserve the first typed query.
- [x] Update `AssignmentsPage` to use the shared query, respond to focus requests, and keep existing filtering behavior intact.
- [x] Add a global `Ctrl+K` shortcut that opens or focuses assignment search without hijacking editable form fields.
- [ ] Verify keyboard, mouse, and cross-view search flows across Dashboard, Assignments, and Grades, then confirm the renderer build still passes.
