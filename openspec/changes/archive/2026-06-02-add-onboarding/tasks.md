## Tasks

- [x] Add onboarding state and persistence so the modal can show once by default and stay dismissed after the user closes it.
- [x] Build the onboarding modal UI with short sections for courses, assignments, completion, and grades.
- [x] Add a clear entry point to reopen onboarding from the authenticated dashboard shell.
- [x] Wire the modal into the signed-in app flow so first-time users see it without blocking normal use.
- [ ] Verify the renderer build and confirm the modal behaves correctly on desktop and mobile layouts.
